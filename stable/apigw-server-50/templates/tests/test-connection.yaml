apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "apigw-server-50.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "apigw-server-50.name" . }}
    helm.sh/chart: {{ include "apigw-server-50.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "apigw-server-50.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
