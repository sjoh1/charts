# Default values for demo.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 2

nginx:
  image:
    repository: nginx
    tag: stable
    containerPort: 80

application:
  image:
    repository: test/apigw-server
    tag: latest
    containerPort: 8080

service:
  type: NodePort
  port: 80

ingress:
  enabled: true
  host: test.apigw-server
  path: "/"
