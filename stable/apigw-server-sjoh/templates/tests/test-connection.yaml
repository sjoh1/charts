apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "apigw-server-sjoh.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "apigw-server-sjoh.name" . }}
    helm.sh/chart: {{ include "apigw-server-sjoh.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "apigw-server-sjoh.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
